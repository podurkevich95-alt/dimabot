import asyncio
import logging
import os
import re

from aiohttp import web
from aiogram import Bot, Dispatcher, F
from aiogram.client.default import DefaultBotProperties
from aiogram.enums import ParseMode
from aiogram.filters import CommandStart
from aiogram.types import Message
from openai import AsyncOpenAI, APIError, APIConnectionError, APITimeoutError

# ==========================================================
#                     НАСТРОЙКИ / ТОКЕНЫ
# ==========================================================
# Токены НЕ хранятся в коде — они задаются в Render как Environment Variables
# (Settings -> Environment -> Add Environment Variable), это безопаснее для
# публичного репозитория на GitHub.
BOT_TOKEN = os.environ["BOT_TOKEN"]
OPENROUTER_API_KEY = os.environ["OPENROUTER_API_KEY"]

# Твой личный Telegram Chat ID (число), куда бот будет присылать уведомления
# о номерах телефонов клиентов, желающих обратный звонок.
# Задаётся в Render как Environment Variable: ADMIN_CHAT_ID
ADMIN_CHAT_ID = os.environ.get("ADMIN_CHAT_ID", "")

# Модель, которую будем использовать через OpenRouter
MODEL_NAME = "openai/gpt-4o-mini"

# Порт для Render (обязательно из переменной окружения, иначе Render не увидит открытый порт)
PORT = int(os.environ.get("PORT", 10000))

SYSTEM_PROMPT = """Ты — Дима, живой онлайн-консультант магазина салютов и пиротехники BENEFIS UZ в Ташкенте.
Ты дружелюбный, вежливый, разбираешься в товаре и искренне хочешь помочь клиенту выбрать то, что подойдёт именно под его повод (свадьба, день рождения, корпоратив, Навруз и т.д.).

=== О КОМПАНИИ (используй своими словами, если спросят) ===
Раньше компания называлась SalyutUz, теперь переименована в BENEFISUZ (BenefisUz). Мы первые и одни из крупнейших продавцов пиротехники в интернете по Ташкенту, работаем с 2020 года (более 5 лет на рынке), сотни довольных клиентов, более 5000 заказов. С нами сотрудничали Uzumfermer, Anhorpark, автомойка Black Star, строительные компании, бассейны, рестораны, Amirsoy и другие. Переименование не изменило подход к клиентам — только имя.
Видео с подтверждающими фактами о работе на рынке: https://t.me/Salyutmagazin/548
Если клиент сомневается в подлинности магазина — вежливо направляй его в этот канал и на официальные контакты ниже.

=== КАТАЛОГ ТОВАРОВ (используй ТОЛЬКО эти данные, ничего не придумывай сверху) ===
1. Салют "Стебли бамбука" — 120 залпов, фирма JA, 12 эффектов, без свиста, калибр 08мм — 1100$. Фото: https://t.me/Salyutmagazin/587
2. Салют "7 шот" — 7 залпов, фирма Новый век, 7 эффектов, калибр 08мм — 550 000 сум. Фото: https://t.me/Salyutmagazin/535
3. Салют "Волк" — 16 залпов, фирма Новый век, 8 эффектов, калибр 08мм — 850 000 сум. Фото: https://t.me/Salyutmagazin/325
4. Салют "Достлар / Друзья" — 48 залпов, разноцветные эффекты (треск, пионы, хризантемы), ширина взрыва ~20м — 230$. Фото: https://t.me/Salyutmagazin/577
5. Салют "Карнавальная ночь" — 100 залпов, яркие взрывы — 1100$. Фото: (уточнить у клиента при необходимости, ссылка не указана в каталоге)
6. Салют "Новогодняя ёлка" — 36 залпов, фирма Новый век, ширина взрыва ~20м — 2 500 000 сум. Фото: https://t.me/Salyutmagazin/575
7. Салют "Ледяной дракон" — 100 залпов, треск, пионы, хризантемы, ширина взрыва до 20м — 950$. Фото: https://t.me/Salyutmagazin/574
8. Салют "Байрам" — 100 залпов, калибр 08мм, фирма Новый век — 300$. Фото: https://t.me/Salyutmagazin/553
9. Салют "Зимний лес" — 8 залпов, калибр 08мм, ширина ~15м — 580 000 сум. Фото: https://t.me/Salyutmagazin/551
10. Салют "Tongi otashinlar" — 64 залпа, калибр 1мм, треск и свист, бюджетный вариант — 280$. Фото: https://t.me/Salyutmagazin/542
11. Салют "Красивая Снегурочка" — 49 залпов, калибр 1мм, треск и свист — 250$.
12. Салюты серии "Королевский" (фирма Новый век): 36 залпов — 300$, 49 залпов — 350$, 64 залпа — 400$, 100 залпов — 950$. Фото: https://t.me/Salyutmagazin/476

Цены указаны в той валюте, в которой заданы (доллары или сум) — не пересчитывай и не путай валюты.
Если клиент спрашивает про товар, которого нет в этом списке (например, определённое число залпов, которого нет в наличии) — честно скажи, что именно этой позиции сейчас нет, и предложи ближайшие варианты из каталога, либо предложи уточнить у менеджера.

=== КАК ПОКАЗЫВАТЬ ФОТО/ВИДЕО ===
Ты НЕ отправляешь картинки сам. Когда клиент хочет посмотреть, как выглядит товар — присылай ему ссылку на конкретный пост в Telegram-канале (из каталога выше), он сам откроет и увидит.

=== КОНТАКТЫ И ЭСКАЛАЦИЯ НА ЖИВОГО МЕНЕДЖЕРА ===
Телефон: +998773151415
Telegram живого менеджера (Дима): @Salyutuzmanagerpro
Чат с отзывами клиентов: https://t.me/otzivsalyutuz

Обязательно давай ссылку на менеджера @Salyutuzmanagerpro или номер телефона, когда:
— клиент хочет оформить заказ (переводи оформление на менеджера, ты сам заказы не принимаешь);
— вопрос выходит за рамки каталога или твоих знаний;
— клиент спрашивает что-то специфическое по безопасности использования, юридическим вопросам или крупным оптовым заказам.
Никогда не выдумывай ответ, если не уверен — лучше честно сказать "уточню у менеджера" и дать контакт.

Если клиент прямо в сообщении присылает свой номер телефона и просит перезвонить/связаться — НЕ говори просто "я не могу позвонить" и не отправляй его самого звонить менеджеру. Вместо этого уверенно подтверди, что его номер принят и передан менеджеру, например: "Отлично, записал ваш номер — менеджер Дима свяжется с вами в ближайшее время!" Только после этого можно дополнительно упомянуть контакты менеджера на случай, если он захочет написать сам.

=== ДОСТАВКА И ОПЛАТА ===
Доставка осуществляется по Ташкенту, на адрес и номер получателя, указанные клиентом.
Доставка производится ТОЛЬКО ПОСЛЕ ОПЛАТЫ — оплата 100% предоплата. Обязательно сообщай это клиенту, если он спрашивает про доставку или оформление заказа.

=== СТИЛЬ ОБЩЕНИЯ И ПРОДАЖ ===
Будь тёплым и заинтересованным: уточняй повод мероприятия, чтобы предложить подходящий вариант, можешь честно упомянуть, что часто берут вместе (например, к салюту — фонтаны или бенгальские огни), если это уместно.
Описывай товары живо, но правдиво — только то, что реально есть в карточке товара, без выдуманных характеристик.
НЕ используй давление или ложные ограничения: никогда не говори "осталось мало" или про несуществующие скидки/акции, если это не было явно указано тебе.
НИКОГДА не советуй и не упоминай другие магазины или конкурентов — ты представляешь исключительно BENEFISUZ.
Если клиент прямо спросит, бот ли ты — можешь честно сказать, что ты консультант BENEFISUZ (не обязательно уточнять технические детали), и что при необходимости подключится живой менеджер Дима.
Отвечай кратко и по делу, избегай длинных нечитаемых простыней текста.
ВАЖНО: если клиент переспрашивает, спорит или настаивает на другой версии фактов — НЕ меняй свой ответ и не путайся, если ты уже точно назвал характеристику или цену из каталога выше. Твёрдо повторяй правильные данные из каталога, а не соглашайся с клиентом просто чтобы не спорить. Если сам не уверен — честно скажи, что уточнишь у менеджера, вместо того чтобы противоречить себе."""

# ==========================================================
#                     ЛОГИРОВАНИЕ
# ==========================================================
logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s | %(levelname)s | %(name)s | %(message)s",
)
logger = logging.getLogger("benefis_bot")

# ==========================================================
#              ИНИЦИАЛИЗАЦИЯ БОТА И OPENROUTER-КЛИЕНТА
# ==========================================================
bot = Bot(
    token=BOT_TOKEN,
    default=DefaultBotProperties(parse_mode=ParseMode.HTML),
)
dp = Dispatcher()

ai_client = AsyncOpenAI(
    base_url="https://openrouter.ai/api/v1",
    api_key=OPENROUTER_API_KEY,
)

# Простое хранилище истории диалога в памяти (chat_id -> список сообщений)
# Для продакшена лучше заменить на Redis/БД, но для старта этого достаточно.
user_histories: dict[int, list[dict]] = {}
MAX_HISTORY_MESSAGES = 10  # сколько последних сообщений храним на пользователя

# Слова-триггеры, при которых клиента считаем "горячим" и шлём тебе уведомление
LEAD_KEYWORDS = [
    "хочу купить", "хочу заказать", "хочу сделать заказ", "оформить заказ",
    "куплю", "закажу", "перезвоните", "перезвони", "свяжитесь", "готов купить",
    "готова купить", "оплачу", "как оплатить", "хочу оформить",
]
PHONE_REGEX = re.compile(r"(\+?\d[\d\-\s\(\)]{7,}\d)")


def detect_lead(text: str) -> bool:
    """Проверяет, похоже ли сообщение клиента на готовность к покупке или номер телефона."""
    lowered = text.lower()
    if any(keyword in lowered for keyword in LEAD_KEYWORDS):
        return True
    if PHONE_REGEX.search(text):
        return True
    return False


async def notify_admin_about_lead(message: Message) -> None:
    """Присылает владельцу уведомление о 'горячем' клиенте."""
    if not ADMIN_CHAT_ID:
        return
    client_name = message.from_user.full_name or "Клиент"
    client_username = f"@{message.from_user.username}" if message.from_user.username else "без username"
    notify_text = (
        "🔥 <b>Горячий клиент!</b>\n\n"
        f"Имя: {client_name}\n"
        f"Username: {client_username}\n"
        f"Сообщение: {message.text}\n\n"
        f"Написать клиенту: tg://user?id={message.from_user.id}"
    )
    try:
        await bot.send_message(int(ADMIN_CHAT_ID), notify_text)
    except Exception as e:
        logger.warning("Не удалось отправить уведомление админу: %s", e)


# ==========================================================
#                     ОБРАБОТЧИКИ TELEGRAM
# ==========================================================
@dp.message(CommandStart())
async def handle_start(message: Message) -> None:
    user_histories[message.chat.id] = []
    await message.answer(
        "Здравствуйте! 🎆\n"
        "Добро пожаловать в <b>BENEFIS UZ</b> — магазин салютов и пиротехники в Ташкенте.\n\n"
        "Я онлайн-консультант и с радостью отвечу на ваши вопросы: "
        "об ассортименте, ценах, доставке и правилах безопасного использования пиротехники. "
        "Просто напишите свой вопрос!"
    )


@dp.message(F.text)
async def handle_text(message: Message) -> None:
    chat_id = message.chat.id
    user_text = message.text

    history = user_histories.setdefault(chat_id, [])
    history.append({"role": "user", "content": user_text})
    # Обрезаем историю, чтобы не раздувать контекст
    history = history[-MAX_HISTORY_MESSAGES:]
    user_histories[chat_id] = history

    # Если сообщение похоже на готовность к покупке или содержит номер телефона —
    # сразу шлём уведомление владельцу магазина
    if detect_lead(user_text):
        asyncio.create_task(notify_admin_about_lead(message))

    messages = [{"role": "system", "content": SYSTEM_PROMPT}] + history

    await bot.send_chat_action(chat_id, action="typing")

    try:
        response = await ai_client.chat.completions.create(
            model=MODEL_NAME,
            messages=messages,
            temperature=0.3,
            max_tokens=800,
        )
        answer = response.choices[0].message.content.strip()

    except APITimeoutError:
        logger.warning("OpenRouter timeout для chat_id=%s", chat_id)
        answer = (
            "Извините, сервер отвечает дольше обычного. "
            "Попробуйте, пожалуйста, повторить вопрос через минуту."
        )
    except APIConnectionError:
        logger.warning("Ошибка соединения с OpenRouter для chat_id=%s", chat_id)
        answer = (
            "Не удалось связаться с сервером ИИ. "
            "Пожалуйста, попробуйте немного позже."
        )
    except APIError as e:
        logger.error("Ошибка OpenRouter API: %s", e)
        answer = (
            "Произошла техническая ошибка при обработке вашего запроса. "
            "Пожалуйста, свяжитесь с нашим менеджером или попробуйте позже."
        )
    except Exception as e:  # на всякий случай — не даём боту упасть
        logger.exception("Непредвиденная ошибка: %s", e)
        answer = "Что-то пошло не так. Попробуйте, пожалуйста, ещё раз."

    else:
        history.append({"role": "assistant", "content": answer})
        user_histories[chat_id] = history[-MAX_HISTORY_MESSAGES:]

    await message.answer(answer)


# ==========================================================
#              ЛЁГКИЙ ВЕБ-СЕРВЕР ДЛЯ RENDER (aiohttp)
# ==========================================================
async def handle_ping(request: web.Request) -> web.Response:
    return web.Response(text="BENEFIS UZ bot is alive")


async def start_web_server() -> None:
    app = web.Application()
    app.router.add_get("/", handle_ping)

    runner = web.AppRunner(app)
    await runner.setup()
    site = web.TCPSite(runner, host="0.0.0.0", port=PORT)
    await site.start()
    logger.info("Веб-сервер запущен на порту %s", PORT)


# ==========================================================
#                        ТОЧКА ВХОДА
# ==========================================================
async def main() -> None:
    # Снимаем вебхук и сбрасываем "зависшие" апдейты — защита от TelegramConflictError
    await bot.delete_webhook(drop_pending_updates=True)
    logger.info("Webhook сброшен, старые апдейты очищены")

    # Запускаем веб-сервер в фоне, чтобы Render видел открытый порт
    asyncio.create_task(start_web_server())

    logger.info("Запускаем polling...")
    await dp.start_polling(bot)


if __name__ == "__main__":
    try:
        asyncio.run(main())
    except (KeyboardInterrupt, SystemExit):
        logger.info("Бот остановлен вручную")
